[project]
name = "shadthon"
version = "0.1.0"
description = "Async Python library for Shad Messenger"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}

dependencies = [
    "httpx>=0.27.0",
    "pycryptodome>=3.20.0"
]